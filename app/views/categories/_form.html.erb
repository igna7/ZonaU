<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, placeholder: "Categoría", style: "height:50px;", class:"form-styles" %>
  </div>
  <div class="cfield">
    <br/>
    <%= f.label "Selecciona un color:" %><%= f.color_field :color, style: "height:30px;" "width:200px", class:"colorcategories"%>
  </div>
  <br/>
  <div class="actions">
    <%= f.submit "Crear categoría", class:"btn-submit"%>
  </div>
<% end %>
